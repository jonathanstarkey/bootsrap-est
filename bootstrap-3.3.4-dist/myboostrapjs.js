/// BOOSTRAP TEMPLATE SITE

$(document).ready(function() {
            
            // Parallax
            $(window).stellar();
            
            // Validate form
            $("form").validate();
                        
            
     });